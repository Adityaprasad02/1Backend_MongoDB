<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Read</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-900">
    <a class="text-sky-700 p-5" href="/">Back to create userData</a>
    <div class="flex flex-wrap mt-4 gap-5">
        <% if((readData.length)>0){  %>
   <% readData.forEach(function(value){ %>
    <div class="max-w-sm rounded overflow-hidden ml-8 border border-purple-700 shadow-lg bg-slate-800">
        <img class="w-full p-3 h-48 object-cover" src="<%=value.image%>" alt="Card Image">
        <div class="px-6 py-4">
            <div class="font-bold text-xl mb-2 text-white"><%=value.name%></div>
            <p class="text-gray-300 text-base">
                <%=value.email%>
            </p>
        </div>
        <div class="px-5 pt-5 pb-6 gap-1 flex justify-end font-mono">
            <a href="/edit/<%=value._id%>" class=" text-green-500 font-bold py-2 px-4 rounded">
                Update
            </a>
            <a href="/delete/<%=value._id%>" class=" text-red-500 font-bold py-2 px-4 rounded">
                Delete
            </a>
        </div>
    </div>
    <% }) %>
    <% } else{  %> 
        <div class="text-3xl text-white p-3">No Details to display </div>
   <% }  %> 


</div>
</body>
</html>
